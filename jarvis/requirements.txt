# Базовые зависимости — нужны на обеих платформах
requests
beautifulsoup4

# Офлайн-"мозг" прямо на устройстве (Qwen2.5-1.5B через llama.cpp).
# На слабом телефоне сборка (компиляция) может не завершиться — тогда просто
# не устанавливай, Джарвис пропустит этот "мозг" в цепочке.
llama-cpp-python

# --- Голосовой ввод/вывод (JARVIS_VOICE=1), необязательно ---

# Vosk — офлайн-распознавание речи, нужен на обеих платформах.
vosk

# Запись с микрофона на ПК (готовые wheel-пакеты, компилировать не нужно).
# На Android НЕ устанавливай — используется termux-microphone-record из pkg termux-api.
sounddevice; sys_platform == "win32"

# Озвучка ответов на ПК (через SAPI5). На Android НЕ устанавливай — используется
# termux-tts-speak из pkg termux-api (родной голосовой движок телефона).
pyttsx3; sys_platform == "win32"
